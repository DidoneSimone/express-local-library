extends layout

block content
  h1= title
  
  if book_list.length
    ul
      each book in book_list
        li(class='listaLibri')
          a(class='libro',href=book.url) #{book.title}
          |  (#{book.author.name})

  else
    p There are no books.
